// TODO: challenge flow (POST /api/duel/challenge) + real-time duel UI via socket.io client

import DashboardLayout from '../components/DashboardLayout.jsx';

const Duel = () => {
  return (
    <DashboardLayout>
      <h1 className="text-2xl font-bold text-gray-900 tracking-tight mb-1">Duel</h1>
      <p className="text-gray-500 text-sm">TODO</p>
    </DashboardLayout>
  );
};

export default Duel;
